# Un blocco per progetto. Aggiungine uno nuovo qui sotto ogni volta che aggiungi
# uno stack sul VPS — non serve toccare nient'altro (Caddy fa reload da solo se lanci
# `docker exec caddy caddy reload --config /etc/caddy/Caddyfile`, oppure ricrea il container).

weather-malcesine.davide-turrini.it {
	reverse_proxy malcesine-meteo-web-prod-1:80
}
